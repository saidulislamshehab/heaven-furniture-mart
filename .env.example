# Server-side only — never prefix with VITE_ (that would expose it to the browser).
OPENROUTER_API_KEY=
# Optional. Comma-separated list; first is primary, the rest are fallbacks.
OPENROUTER_MODEL=nvidia/nemotron-3-ultra-550b-a55b:free,minimax/minimax-m2.7:free,google/gemma-4-31b-it:free
